version: '3.8'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - WEBHOOK_SECRET=${WEBHOOK_SECRET:-testsecret}
      - DATABASE_URL=/data/app.db
      - LOG_LEVEL=INFO
    volumes:
      - app_data:/data

volumes:
  app_data:
